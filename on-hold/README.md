# on-hold — parked work

This folder holds tasks and items **postponed for later**, so they don't clutter the active project but aren't forgotten. Each entry: what it is, why it's parked, and what unblocks it.

---

## Parked items

### 1. Convert proposal + journal docs to Word (.docx) / PDF
- **What:** `docs/AI_ChemEng_Research_Gap_and_Proposal.md` and `docs/target_journals.md` into formatted `.docx` (and/or PDF) for sharing with advisor / submission prep.
- **Why parked:** the Linux sandbox needed to run the document generator failed to start this session (host virtualization disabled — `HYPERVISOR_VIRT_DISABLED`), not a setup problem on your side.
- **Unblocks when:** sandbox is available again in a later session. Then regenerate directly to `docs/`.

### 2. (add future parked items below)
- **What:**
- **Why parked:**
- **Unblocks when:**
